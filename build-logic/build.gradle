plugins {
    id "java-gradle-plugin"
}

gradlePlugin {
    plugins {
        register("embulk-plugin-test") {
            id = "embulk-plugin-test"
            implementationClass = "org.embulk.junit5.EmbulkPluginTestPlugin"
        }
    }
}
